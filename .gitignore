# Secrets — never commit. The .env.* glob catches backups and variants
# (.env.bak, .env.local, .env.save); .env.example is the one exception.
.env
.env.*
!.env.example

# Bot state and local output
state.json
runs/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Autopilot local state (machine-only; see CLAUDE-autopilot.md)
autopilot-wip.md

# Bot runtime state and secrets
state.db
state.db-journal
players.json
backfill-plan.md
